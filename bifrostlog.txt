Bifrost: help
usage: git-bifrost <command> [options]

Commands:
   activate    installs git-bifrost into the current git repo
   clone       like a normal git-clone but installs git-bifrost prior to checkout
   init        like a normal git-init but installs git-bifrost as well
usage: git-bifrost <command> [options]

Commands:
   activate    installs git-bifrost into the current git repo
   clone       like a normal git-clone but installs git-bifrost prior to checkout
   init        like a normal git-init but installs git-bifrost as well
